package com.braydenwhitlock.gametracker.tagdescription;

/** Who wrote a {@link TagDescription}'s current text, or PENDING if no one has yet. */
public enum TagSource {
    AI,
    USER,
    PENDING
}
