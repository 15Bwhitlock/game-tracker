# Backend (Maven)
backend/target/
backend/.mvn/wrapper/maven-wrapper.jar
backend/.classpath
backend/.project
backend/.settings/
backend/*.iml
backend/.idea/

# Frontend (Angular)
frontend/node_modules/
frontend/node/
frontend/dist/
frontend/.angular/
frontend/.idea/
frontend/*.iml

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Editor / tool-local settings (per-machine, not project config)
.claude/settings.local.json
.vscode/settings.json

# Secrets — never commit real tokens/keys. backend/.env holds BGG_API_TOKEN locally.
.env
*.env.local
